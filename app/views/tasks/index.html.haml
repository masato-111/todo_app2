%h1 タスク一覧

=link_to 'タスク登録' , new_task_path, class: 'btn btn-primary'

.list-group.mt-3
  - @tasks.each do |task|
    = link_to task.title, task_path(task), class: 'list-group-item list-group-item-action'
  


