%nav.navbar.bg-body-tertiary
  .container-fluid
    = link_to 'TODO APP', root_path, class: 'navbar-brand'
    %ul.navbar-nav
      %li.nav-item
        - if user_signed_in?
          = button_to 'ログアウト', destroy_user_session_path, class: 'nav-link', method: :delete, data: { turbo_confirm: 'ログアウトしますか？' }
        - else
          = link_to 'ログイン', new_user_session_path, class: 'nav-link'